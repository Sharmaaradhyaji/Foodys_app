import { StyleSheet } from "react-native";
import { height, wp } from "../../globals/globals";

export const stylesHeading = StyleSheet.create({
    heading: {
        fontSize: height * 0.03,
        fontWeight: '500',
        margin: wp('1%'),
        marginLeft: wp("1%")
    }   
})